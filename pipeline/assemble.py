"""Assemble all human-tagged episode YAMLs into an in-memory dataset.

W0 stub — implementation lands in W1. Reads every ``data/episodes/*.yaml``,
parses it, and returns a list of dicts suitable for schema validation and JSON
serialization.
"""
