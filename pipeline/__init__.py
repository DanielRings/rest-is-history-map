"""Rest Is History pipeline package.

Produces ``data/episodes.json`` from per-episode YAML files in
``data/episodes/`` and validates against ``schema/episodes.schema.json``.

Subpackages are stubs at W0; implementation lands in W1.
"""

__version__ = "0.1.0"
