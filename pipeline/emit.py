"""Write the validated dataset to ``data/episodes.json``.

W0 stub — implementation lands in W1. Output is a single JSON object with
``version``, ``generated_at`` (UTC ISO-8601), and ``episodes`` keys, matching
the schema.
"""
