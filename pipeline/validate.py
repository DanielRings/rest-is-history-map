"""Validate the assembled dataset against ``schema/episodes.schema.json``.

W0 stub — implementation lands in W1. Raises ``jsonschema.ValidationError`` on
any violation per the CLAUDE.md "never return default/fallback" rule.
"""
