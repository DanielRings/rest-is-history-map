"""Command-line entry point for the pipeline.

W0 stub — implementation lands in W1. Subcommands:
  * ``rih-pipeline refresh`` — fetch RSS, write new pending stubs.
  * ``rih-pipeline build``   — assemble + validate + emit ``data/episodes.json``.
"""


def main() -> None:
    """Pipeline CLI entry point. Wired up in W1."""
    raise NotImplementedError("rih-pipeline CLI is scaffolded in W0; implemented in W1.")
